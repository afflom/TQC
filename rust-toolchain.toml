# Use the stable channel (matches the holospaces substrate convention).
# MSRV is declared in each crate's `rust-version` (1.83) and enforced by a dedicated
# CI job; cross-compilation targets are added in CI rather than forced on every dev.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
