# The substrate (holospaces) consumes `hologram` as a git dependency; use the git CLI
# so credential helpers and the org token (in CI) are honored when that dep is enabled.
[net]
git-fetch-with-cli = true
